#include <cstdio>
#include <cstdlib>
#include "myio.h"
#include "myexec.h"

int main(int argc,char** argv)
{
    //Run_shell();
    while(true)
    {
        char *rd;
        rd = myreadline(stdin);
    }
    return 0;
}